name: Baguette CI2

on:
    push:
        branches:
            - main
    discussion:
        types: [created]
    workflow_dispatch:
    
jobs:
    hello_world_job:
        runs-on: ubuntu-latest
        steps:
            - name: Dog
              uses: actions/checkout@v2
            - name: Dog2
              run: echo ${{github.event}}
