name: Baguette CI2

on:
    discussion:
        types: [created, edited]
    workflow_dispatch:
    
jobs:
    hello_world_job:
        runs-on: ubuntu-latest
        steps:
            - name: Dog
              uses: actions/checkout@v2
            - name: Dog2
              run: echo ${{github.event}}
